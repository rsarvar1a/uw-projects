int wain (int a, int b)
{
  return 
  a + b - a * b / a % 
  ( b + a - b * a / b % 
    ( a + b - a * b / a % 
      ( b + a - b * a / b % 
        (a + b - a * b / a % 
          (b + a - b * a / b % 
            (a + b - a * b / a % 
              (b + a - b * a / b %
                (a + b - a * b / a % 
                  (b + a - b * a / b % a 
                  )
                )
              )
            )
          )
        )
      ) 
    )
  ); 

}
