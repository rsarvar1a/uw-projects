int wain (int a, int b)
{
  int c = 0;
  int d = 0;

  c = (a + b) / a;
  d = a + (b / a);

  println((a + a) % (b / a));

  b = (b + b) - (b + b) + (c * (a + a));
  a = (d - a) + (((a)));

  if ((a + (a / b)) == ((a + a) / b))
  {
    println(1);
  }
  else
  {
    println(0);
  }

  return 0;
}
