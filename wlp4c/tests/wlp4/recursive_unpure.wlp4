int p (int * a)
{
  *a = *a + 1;
  return 0;
}

int sum (int * a, int n, int i)
{
  int ret = 0;
  if (i < n)
  {
    ret = p(a) + sum(a, n, i + 1) + sum(a, n, i + 1);
  }
  else
  {
    println((0 - 1));
  }
  return ret;
}

int wain (int *a, int b)
{
  int ret = 0;
  ret = sum(a, b, 0);
  println(*a);
  return ret;
}
